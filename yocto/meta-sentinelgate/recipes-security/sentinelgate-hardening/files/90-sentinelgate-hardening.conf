# Conservative host hardening for an embedded SentinelGate image.
# Deliberately does not change IP forwarding, because a firewall/gateway may need it.

# Reduce kernel information disclosure.
kernel.dmesg_restrict = 1
kernel.kptr_restrict = 2

# Harden link/FIFO handling against common local privilege-abuse patterns.
fs.protected_hardlinks = 1
fs.protected_symlinks = 1
fs.protected_fifos = 2
fs.protected_regular = 2

# Restrict unprivileged BPF where the kernel supports this setting.
kernel.unprivileged_bpf_disabled = 1

# Do not accept ICMP redirects on managed interfaces.
net.ipv4.conf.all.accept_redirects = 0
net.ipv4.conf.default.accept_redirects = 0
net.ipv4.conf.all.secure_redirects = 0
net.ipv4.conf.default.secure_redirects = 0
net.ipv6.conf.all.accept_redirects = 0
net.ipv6.conf.default.accept_redirects = 0
